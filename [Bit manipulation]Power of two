SOlution:
bool isPowerOfTwo(int n) {
        ios::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
        if( n <= 0 || n & (n-1))
            return false;
        else
            return true;
 }
